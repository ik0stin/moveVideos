export function px(size) {
  const baseFontSize = 16;
  return `${size / baseFontSize}rem`;
}

export function mob(size) {
  const baseFontSize = 4.166;
  return `${size / baseFontSize}rem`;
}

// export default pxToRem;
